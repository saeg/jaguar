language: java

sudo: false

jdk:
  - oraclejdk8

before_install:
  - make prepare

install: make build_core

after_success:
  - coveralls

cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.m2/wrapper'
    